name: "Hello"

on: [workflow_dispatch]
   
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ./
        name: calling custom action
        with: 
          message: Avinash



      - uses: ./actions/my-action-1
        name: calling custom action in the dir
        with: 
          message: Avinash